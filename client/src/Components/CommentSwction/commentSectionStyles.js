import { makeStyles } from "@material-ui/core";

export default makeStyles({
    userAvatar: {
        borderRadius: '100px', 
        maxWidth: '40px',
        margin: '0 10px 0 0'
      },
      heighPartOfComment: {
          display: 'flex',
          alignItems: 'center',
          padding: '10px'
      }

})